yapi: v1

default_environment: local

environments:
  local:
    url: http://localhost:3000

  prod:
    url: https://yapi.run

test:
  start: pnpm dev --port 3000
  wait_on:
    - http://localhost:3000/healthz
  timeout: 60s
  parallel: 16
  all: true
